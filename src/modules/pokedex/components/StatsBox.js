import React from 'react';
import styled from 'styled-components';
const Styled = {};
Styled.Wrapper = styled.div`

`;
const StatsBox = ({className}) => {
  return (
    <Styled.Wrapper className={className}>
    
    </Styled.Wrapper>
  );
};

export default StatsBox;
